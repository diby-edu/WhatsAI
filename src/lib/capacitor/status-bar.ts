export async function initStatusBar() {
    // Status bar setup is now handled natively in MainActivity for Android 15+ edge-to-edge.
    // Keeping this function as a no-op avoids regressions in existing component wiring.
    return
}

